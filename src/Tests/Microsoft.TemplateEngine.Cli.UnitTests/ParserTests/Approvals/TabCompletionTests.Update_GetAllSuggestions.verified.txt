[
  {
    Label: --add-source,
    Kind: Keyword,
    SortText: --add-source,
    InsertText: --add-source,
    Detail: Specifies a NuGet source to use.
  },
  {
    Label: --check-only,
    Kind: Keyword,
    SortText: --check-only,
    InsertText: --check-only,
    Detail: Only checks for updates and display the template packages to be updated without applying update.
  },
  {
    Label: --dry-run,
    Kind: Keyword,
    SortText: --dry-run,
    InsertText: --dry-run,
    Detail: Only checks for updates and display the template packages to be updated without applying update.
  },
  {
    Label: --interactive,
    Kind: Keyword,
    SortText: --interactive,
    InsertText: --interactive,
    Detail: Allows the command to stop and wait for user input or action (for example to complete authentication).
  },
  {
    Label: --nuget-source,
    Kind: Keyword,
    SortText: --nuget-source,
    InsertText: --nuget-source,
    Detail: Specifies a NuGet source to use.
  }
]