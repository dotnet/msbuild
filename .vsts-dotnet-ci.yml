phases:
# - phase: LinuxMono
#   queue: Hosted Linux Preview
#   steps:
#   - bash: . 'build/install-mono.sh'
#     displayName: Install Mono
#   - bash: . 'build/cibuild.sh' '-hostType mono'
#     displayName: CI Build
#   - task: PublishTestResults@2
#     displayName: Publish Test Results
#     inputs:
#       testRunner: XUnit
#       testResultsFiles: 'artifacts/**/*UnitTests*.xml'
#     condition: always()

# - phase: LinuxCoreCLR
#   queue: Hosted Linux Preview
#   steps:
#   - bash: . 'build/cibuild.sh'
#     displayName: CI Build
#   - task: PublishTestResults@2
#     displayName: Publish Test Results
#     inputs:
#       testRunner: XUnit
#       testResultsFiles: 'artifacts/**/*UnitTests*.xml'
#     condition: always()

# - phase: Mac
#   queue: 'Hosted macOS Preview'
#   steps:
#   - bash: . 'build/cibuild.sh'
#     displayName: CI Build
#   - task: PublishTestResults@2
#     displayName: Publish Test Results
#     inputs:
#       testRunner: XUnit
#       testResultsFiles: 'artifacts/**/*UnitTests*.xml'
#     condition: always()

- phase: WindowsCore
  queue: 'Hosted VS2017'
  steps:
  - script: set
    displayName: Command Line Script
  - task: BatchScript@1
    displayName: VsDevCmd
    inputs:
      filename: '$(agent.VisualStudio)\Common7\Tools\VsDevCmd.bat'
      arguments: '-hostType Core'
      modifyEnvironment: true
  - task: BatchScript@1
    displayName: cibuild.cmd
    inputs:
      filename: 'build/cibuild.cmd'
      arguments: '-hostType Core'
  - task: PublishTestResults@2
    displayName: Publish Test Results
    inputs:
      testRunner: XUnit
      testResultsFiles: 'artifacts/**/*UnitTests*.xml'
    condition: always()